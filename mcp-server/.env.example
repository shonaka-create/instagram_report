# publish_report(レポート公開)用。Next.js側の .env.local と同じ値でよい
SUPABASE_URL=https://nffprofzjzvkxsxyegdq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# レポートURLのベース。Vercelデプロイ後はそのURL、ローカル確認中は http://localhost:3000
APP_URL=http://localhost:3000

# clients.json の場所を変えたい場合のみ設定(省略時は mcp-server/clients.json)
# CLIENTS_FILE=C:\path\to\clients.json
